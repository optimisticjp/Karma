name: Generate content migration

on:
  push:
    branches:
      - feat/console-reports-content-polish
    paths:
      - src/lib/db/content-schema.ts
      - drizzle.config.ts

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/console-reports-content-polish
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Generate Drizzle migration
        run: npx drizzle-kit generate --name=content_desk
      - name: Commit generated migration
        run: |
          if [ -z "$(git status --porcelain drizzle)" ]; then
            echo "No migration changes generated"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add drizzle
          git commit -m "db: add content desk migration"
          git push origin HEAD:feat/console-reports-content-polish
