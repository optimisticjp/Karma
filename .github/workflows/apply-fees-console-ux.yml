name: Apply front desk fees UX patch

on:
  push:
    branches:
      - feat/fees-and-console-ux
    paths:
      - .github/workflows/apply-fees-console-ux.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/fees-and-console-ux
      - name: Apply front desk UX changes
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import json
          import re

          def read(path):
              return Path(path).read_text()

          def write(path, text):
              Path(path).write_text(text)

          def replace(path, old, new):
              text = read(path)
              if old not in text:
                  raise SystemExit(f"missing replacement anchor in {path}: {old[:100]!r}")
              if text.count(old) != 1:
                  raise SystemExit(f"replacement anchor not unique in {path}: {old[:100]!r}")
              write(path, text.replace(old, new))

          # -----------------------------------------------------------------
          # Public admission: stale restored drafts must never submit a course
          # slot that no longer belongs to the selected/context course, and a
          # typed API failure should give the visitor a useful recovery path.
          # -----------------------------------------------------------------
          p = "src/components/forms/AdmissionForm.tsx"
          replace(p,
          '  const [serverError, setServerError] = useState(false);',
          '  const [serverError, setServerError] = useState<{ message: string; requestId?: string } | null>(null);')

          replace(p,
          '''    if (validContextCourse) {
      next = {
        ...next,
        courseSlug: validContextCourse,
        preferredTiming: context?.timing ?? next.preferredTiming
      };
      setFromContext(true);
    }
    setData(next);
    setStep(nextStep);''',
          '''    let resetToCourseStep = false;
    if (validContextCourse) {
      const courseChanged = next.courseSlug !== validContextCourse;
      next = {
        ...next,
        courseSlug: validContextCourse,
        preferredTiming: context?.timing ?? next.preferredTiming,
        preferredSchedule: courseChanged ? "" : next.preferredSchedule,
        demoSlot: courseChanged ? "" : next.demoSlot
      };
      resetToCourseStep = courseChanged;
      setFromContext(true);
    }

    /* A saved draft can outlive a staff timetable/course edit. Never carry a
       stale slot into the review step: the API correctly rejects it, but the
       visitor would otherwise see only a generic failure for a hidden value. */
    const restoredCourse = courses.find((course) => course.slug === next.courseSlug);
    if (!restoredCourse && next.courseSlug) {
      next = { ...next, courseSlug: "", preferredSchedule: "", demoSlot: "" };
      resetToCourseStep = true;
    } else if (restoredCourse) {
      if (
        next.preferredSchedule &&
        !restoredCourse.scheduleOptions.some((slot) => slot.key === next.preferredSchedule)
      ) {
        next = { ...next, preferredSchedule: "" };
        resetToCourseStep = true;
      }
      if (next.demoSlot && !restoredCourse.demoSlots.some((slot) => slot.key === next.demoSlot)) {
        next = { ...next, demoSlot: "" };
      }
    }
    setData(next);
    setStep(resetToCourseStep ? 0 : nextStep);''')

          replace(p,
          '''  const onSubmit = async () => {
    const e = validate(3);
    setErrors(e);
    if (Object.keys(e).length > 0) {
      requestAnimationFrame(() => focusField(Object.keys(e)[0]));
      return;
    }
    setBusy(true);
    setServerError(false);
    try {''',
          '''  const onSubmit = async () => {
    /* Revalidate every step at the final boundary. A draft can be restored
       after the catalogue changed, so "the visitor already passed step 1" is
       not a safe assumption. */
    const e = Object.assign({}, validate(0), validate(1), validate(2), validate(3));
    setErrors(e);
    const errorKeys = Object.keys(e);
    if (errorKeys.length > 0) {
      const first = errorKeys[0];
      const targetStep =
        ["courseSlug", "preferredTiming", "preferredSchedule"].includes(first) ? 0 :
        ["fullName", "whatsapp", "guardianPhone"].includes(first) ? 1 :
        ["ageBand", "guardianName", "referencePhone", "occupation", "experience", "area"].includes(first) ? 2 : 3;
      if (targetStep !== step) go(targetStep);
      requestAnimationFrame(() => focusField(first));
      return;
    }
    setBusy(true);
    setServerError(null);
    try {''')

          replace(p,
          '''      const out = (await res.json()) as { ok: boolean; reference?: string; waUrl?: string };
      if (!res.ok || !out.ok || !out.reference) throw new Error("submit failed");
      const waUrl = out.waUrl ?? waLink(t("success.waMessage", { ref: out.reference }));''',
          '''      const out = (await res.json()) as {
        ok: boolean;
        reference?: string;
        waUrl?: string;
        error?: string;
        requestId?: string;
      };
      if (!res.ok || !out.ok || !out.reference) {
        const message =
          out.error === "validation"
            ? t("errors.serverValidation")
            : out.error === "turnstile" || out.error === "turnstile_unavailable"
              ? t("errors.security")
              : out.error === "rate_limited"
                ? t("errors.rateLimited")
                : out.error === "service_unavailable"
                  ? t("errors.service")
                  : t("errors.generic");
        setServerError({ message, requestId: out.requestId });
        return;
      }
      const waUrl = out.waUrl ?? waLink(t("success.waMessage", { ref: out.reference }));''')

          replace(p,
          '''    } catch {
      setServerError(true);
    } finally {''',
          '''    } catch {
      setServerError({ message: t("errors.service") });
    } finally {''')

          replace(p,
          '''            {serverError ? (
              <p role="alert" className="field-error">
                {t("errors.generic")}
              </p>
            ) : null}''',
          '''            {serverError ? (
              <div role="alert" className="field-error">
                <p>{serverError.message}</p>
                {serverError.requestId ? (
                  <p className="mt-1">{t("errors.supportRef", { ref: serverError.requestId })}</p>
                ) : null}
              </div>
            ) : null}''')

          for path, additions in {
              "messages/en.json": {
                  "rateLimited": "Too many attempts just now. Wait a few minutes, then try again.",
                  "security": "The security check expired or could not be verified. Wait for it to refresh, then try again.",
                  "serverValidation": "Some saved form details are no longer valid. Go back once, reselect the course and timing, then submit again.",
                  "service": "The application service is temporarily unavailable. Your form is still saved on this device; please try again shortly.",
                  "supportRef": "Support reference: {ref}."
              },
              "messages/gu.json": {
                  "rateLimited": "હમણાં બહુ attempts થયા છે. થોડા મિનિટ પછી ફરી try કરો.",
                  "security": "Security check expire થયું છે અથવા verify થયું નથી. તે refresh થાય પછી ફરી try કરો.",
                  "serverValidation": "Formમાં saved થયેલી કેટલીક વિગતો હવે valid નથી. એક વાર પાછળ જઈ course અને timing ફરી પસંદ કરીને submit કરો.",
                  "service": "હમણાં application service ઉપલબ્ધ નથી. તમારું form આ device પર saved છે; થોડી વાર પછી ફરી try કરો.",
                  "supportRef": "Support reference: {ref}."
              }
          }.items():
              data = json.loads(read(path))
              errors = data["admissionForm"]["errors"]
              errors.update(additions)
              write(path, json.dumps(data, ensure_ascii=False, indent=2) + "\n")

          # -----------------------------------------------------------------
          # Fees domain: daily payment input no longer carries the agreement
          # amount. The server derives it from the enrolment snapshot/legacy
          # ledger and keeps discounts monotonic because historical rows are
          # immutable.
          # -----------------------------------------------------------------
          p = "src/lib/admin/fees.ts"
          marker = '/* --------------------------- the agreement -------------------------------- */'
          addition = r'''
export type PaymentEntryInput = {
  enrollmentId: number;
  received: number;
  discount: number | null;
  method: FeeMethod | null;
  receiptNo: string | null;
  dueDate: string | null;
  notes: string | null;
};

/** Daily front-desk receipt input. The agreed fee never comes from this form. */
export function validatePaymentEntry(input: Record<string, unknown>):
  | { ok: true; value: PaymentEntryInput }
  | { ok: false } {
  const enrollmentId = positiveId(input.enrollmentId);
  const received = amount(input.received);
  const discount = input.discount == null || input.discount === "" ? null : amount(input.discount);
  const dueDate = optionalDate(input.dueDate);
  const rawMethod = optionalText(input.method, 30);
  const method = rawMethod && FEE_METHODS.includes(rawMethod as FeeMethod) ? rawMethod as FeeMethod : null;
  if (!enrollmentId || received == null || discount === null && input.discount != null && input.discount !== "" || dueDate === "invalid") return { ok: false };
  if (received > 0 && !method) return { ok: false };
  return {
    ok: true,
    value: {
      enrollmentId,
      received,
      discount,
      method,
      receiptNo: optionalText(input.receiptNo, 40),
      dueDate,
      notes: optionalText(input.notes, 300)
    }
  };
}

export type AdmissionFeeSetupInput = {
  agreedFeeTotal: number | null;
  agreedAdmissionAmount: number | null;
  agreedBalanceDueOn: string | null;
  discount: number;
  received: number;
  method: FeeMethod | null;
  receiptNo: string | null;
  notes: string | null;
};

/** Optional commercial terms entered while a walk-in is admitted. */
export function validateAdmissionFeeSetup(input: Record<string, unknown>):
  | { ok: true; value: AdmissionFeeSetupInput }
  | { ok: false } {
  const total = input.agreedFeeTotal == null || input.agreedFeeTotal === "" ? null : amount(input.agreedFeeTotal);
  const admission = input.agreedAdmissionAmount == null || input.agreedAdmissionAmount === "" ? null : amount(input.agreedAdmissionAmount);
  const dueOn = optionalDate(input.agreedBalanceDueOn);
  const discount = amount(input.feeDiscount ?? 0);
  const received = amount(input.feeReceived ?? 0);
  const rawMethod = optionalText(input.feeMethod, 30);
  const method = rawMethod && FEE_METHODS.includes(rawMethod as FeeMethod) ? rawMethod as FeeMethod : null;
  if (dueOn === "invalid" || discount == null || received == null) return { ok: false };
  if (input.agreedFeeTotal != null && input.agreedFeeTotal !== "" && total == null) return { ok: false };
  if (input.agreedAdmissionAmount != null && input.agreedAdmissionAmount !== "" && admission == null) return { ok: false };
  if (received > 0 && !method) return { ok: false };
  return {
    ok: true,
    value: {
      agreedFeeTotal: total,
      agreedAdmissionAmount: admission,
      agreedBalanceDueOn: dueOn,
      discount,
      received,
      method,
      receiptNo: optionalText(input.feeReceiptNo, 40),
      notes: optionalText(input.feeNote, 300)
    }
  };
}

'''
          text = read(p)
          if marker not in text:
              raise SystemExit("fees agreement marker missing")
          write(p, text.replace(marker, addition + marker, 1))

          p = "src/app/admin/(console)/fees/actions.ts"
          replace(p,
          'import { validateAgreementUpdate, validateFeeRecord } from "@/lib/admin/fees";',
          'import { validateAgreementUpdate, validatePaymentEntry } from "@/lib/admin/fees";')
          replace(p,
          '''    | "overpaid"
    | "belowReceived"
    | "generic";''',
          '''    | "overpaid"
    | "belowReceived"
    | "agreementMissing"
    | "discountDecrease"
    | "generic";''')

          text = read(p)
          start = text.index('export async function addFeeRecordAction')
          end = text.index('/**\n * Changes what an EXISTING student agreed to pay.', start)
          new_action = r'''export async function addFeeRecordAction(_prev: FeeState, formData: FormData): Promise<FeeState> {
  const auth = await authorizeAction({ permission: "fees.manage" });
  if (!auth.ok) return fail("denied");
  const parsed = validatePaymentEntry(Object.fromEntries(formData.entries()));
  if (!parsed.ok) return fail("invalid");
  const db = getDb();
  if (!db) return fail("generic");

  try {
    const d = parsed.value;
    const enrollment = await db
      .select({ id: schema.enrollments.id, agreedFeeTotal: schema.enrollments.agreedFeeTotal })
      .from(schema.enrollments)
      .where(eq(schema.enrollments.id, d.enrollmentId))
      .limit(1);
    if (!enrollment[0]) return fail("missing");

    const previous = await db
      .select({
        received: schema.feeRecords.received,
        discount: schema.feeRecords.discount,
        courseFee: schema.feeRecords.courseFee
      })
      .from(schema.feeRecords)
      .where(eq(schema.feeRecords.enrollmentId, d.enrollmentId))
      .orderBy(desc(schema.feeRecords.createdAt));

    const agreedFeeTotal = enrollment[0].agreedFeeTotal ?? previous[0]?.courseFee ?? null;
    if (agreedFeeTotal == null) return fail("agreementMissing");
    const alreadyReceived = previous.reduce((sum, row) => sum + row.received, 0);
    const currentDiscount = previous.reduce((max, row) => Math.max(max, row.discount), 0);
    if (d.discount != null && d.discount < currentDiscount) return fail("discountDecrease");
    const discount = d.discount ?? currentDiscount;
    if (discount > agreedFeeTotal) return fail("invalid");
    const netFee = Math.max(0, agreedFeeTotal - discount);
    if (alreadyReceived + d.received > netFee) return fail("overpaid");

    await db.transaction(async (tx) => {
      const inserted = await tx.insert(schema.feeRecords).values({
        enrollmentId: d.enrollmentId,
        courseFee: agreedFeeTotal,
        discount,
        received: d.received,
        method: d.method,
        receiptNo: d.receiptNo,
        dueDate: d.dueDate,
        notes: d.notes
      }).returning({ id: schema.feeRecords.id });
      const id = inserted[0]?.id;
      if (!id) throw new Error("fee record insert returned no id");
      const receiptNo = d.receiptNo ?? (d.received > 0 ? `KDS-R-${new Date().getFullYear()}-${pad(id)}` : null);
      if (receiptNo && !d.receiptNo) await tx.update(schema.feeRecords).set({ receiptNo }).where(eq(schema.feeRecords.id, id));
      await tx.insert(schema.auditLogs).values(auditValues({
        actor: String(auth.session.staff.id),
        action: FEE_AUDIT_ACTIONS.recordCreated,
        entity: "fee_record",
        entityId: id,
        newValue: {
          enrollmentId: d.enrollmentId,
          courseFee: agreedFeeTotal,
          discount,
          received: d.received,
          method: d.method,
          receiptNo,
          dueDate: d.dueDate
        },
        reason: d.notes ?? "offline fee ledger entry"
      }));
    });
  } catch (error) {
    console.error("[fees] save failed", error instanceof Error ? error.message : "unknown");
    return fail("generic");
  }

  revalidatePath("/admin/fees");
  revalidatePath("/admin/students");
  revalidatePath("/admin");
  return ok("saved");
}

'''
          write(p, text[:start] + new_action + text[end:])

          p = "src/app/admin/(console)/fees/FeeForm.tsx"
          text = read(p)
          start = text.index('export function FeeEntryForm')
          end = text.index('function Field(', start)
          payment_form = r'''export function FeeEntryForm({
  enrollmentId,
  discount,
  dueDate,
  copy
}: {
  enrollmentId: number;
  discount: number;
  dueDate: string | null;
  copy: FeesCopy;
}) {
  const [state, action] = useActionState<FeeState, FormData>(addFeeRecordAction, IDLE);
  return (
    <form action={action} className="grid gap-4">
      <input type="hidden" name="enrollmentId" value={enrollmentId} />
      <Message state={state} copy={copy} />
      <p className="form-note">{copy.paymentHint}</p>
      <div className="grid gap-4 sm:grid-cols-2">
        <Field label={copy.receivedNow} htmlFor={`fee-received-${enrollmentId}`}>
          <input id={`fee-received-${enrollmentId}`} name="received" type="number" min={0} step={1} className="input" required defaultValue={0} inputMode="numeric" />
        </Field>
        <Field label={copy.method} htmlFor={`fee-method-${enrollmentId}`}>
          <select id={`fee-method-${enrollmentId}`} name="method" className="input" defaultValue="">
            <option value="">—</option>{FEE_METHODS.map((method) => <option key={method} value={method}>{copy.methods[method]}</option>)}
          </select>
        </Field>
      </div>
      <div className="grid gap-4 sm:grid-cols-2">
        <Field label={copy.dueDate} htmlFor={`fee-due-${enrollmentId}`}>
          <input id={`fee-due-${enrollmentId}`} name="dueDate" type="date" className="input" defaultValue={dueDate ?? ""} />
        </Field>
        <Field label={copy.notes} htmlFor={`fee-note-${enrollmentId}`}>
          <input id={`fee-note-${enrollmentId}`} name="notes" className="input" maxLength={300} />
        </Field>
      </div>
      <details className="border-t border-rule pt-3">
        <summary className="cursor-pointer text-smallmeta font-semibold">{copy.moreOptions}</summary>
        <div className="mt-4 grid gap-4 sm:grid-cols-2">
          <Field label={copy.discountTotal} htmlFor={`fee-discount-${enrollmentId}`}>
            <input id={`fee-discount-${enrollmentId}`} name="discount" type="number" min={discount} step={1} className="input" defaultValue={discount} inputMode="numeric" />
            <p className="form-note mt-1">{copy.discountHint}</p>
          </Field>
          <Field label={copy.receiptNo} htmlFor={`fee-receipt-${enrollmentId}`}>
            <input id={`fee-receipt-${enrollmentId}`} name="receiptNo" className="input" maxLength={40} />
          </Field>
        </div>
      </details>
      <div><Submit copy={copy} /></div>
    </form>
  );
}

'''
          write(p, text[:start] + payment_form + text[end:])

          # Bilingual front-desk fee copy.
          p = "src/lib/admin/fees-copy.ts"
          replace(p,
          '  show: "Show",\n  totalAgreed: "Net fees",',
          '  show: "Show",\n  filterStatus: "Show students",\n  filterAll: "Everyone",\n  filterPending: "Pending fees",\n  filterOverdue: "Overdue",\n  filterPaid: "Paid in full",\n  overdueCount: "Overdue students",\n  totalAgreed: "Net fees",')
          replace(p,
          '  recordPayment: "Record fee / payment",\n  save: "Add ledger entry",',
          '  recordPayment: "Record payment",\n  paymentHint: "Enter only what you received today. The student’s agreed fee stays unchanged.",\n  moreOptions: "More options",\n  discountTotal: "Total discount / concession",\n  discountHint: "Keep the current amount unless the institute is giving an additional concession. Recorded discounts are never silently reduced.",\n  agreementNeeded: "Set this student’s agreed fee before recording a payment.",\n  openLedger: "Open full fee ledger",\n  admissionSetupTitle: "Fees for this admission",\n  admissionSetupHint: "Set this student’s fee agreement now, and record today’s payment if money was received. Leave the agreement fields blank to use the course default.",\n  courseDefaultNote: "Course defaults are shown when available. Changing these values affects this student only.",\n  save: "Record payment",')
          replace(p,
          '    belowReceived: "The agreed total cannot be less than the money already received.",\n    generic:',
          '    belowReceived: "The agreed total cannot be less than the money already received.",\n    agreementMissing: "Set the agreed fee for this student first, then record the payment.",\n    discountDecrease: "A recorded discount cannot be reduced from a new receipt. Use the audited agreement change if the student’s terms need correcting.",\n    generic:')
          replace(p,
          '  show: "બતાવો",\n  totalAgreed: "Net fees",',
          '  show: "બતાવો",\n  filterStatus: "Students બતાવો",\n  filterAll: "બધા",\n  filterPending: "Fees બાકી",\n  filterOverdue: "તારીખ વીતી ગઈ",\n  filterPaid: "પૂરી ભરાઈ",\n  overdueCount: "Overdue students",\n  totalAgreed: "Net fees",')
          replace(p,
          '  recordPayment: "Fee / Payment નોંધો",\n  save: "Ledger entry ઉમેરો",',
          '  recordPayment: "Payment નોંધો",\n  paymentHint: "આજે જેટલી રકમ મળી છે એટલી જ નાખો. Studentની નક્કી થયેલી fee બદલાશે નહીં.",\n  moreOptions: "વધુ options",\n  discountTotal: "કુલ discount / concession",\n  discountHint: "હાલનું amount એમ જ રાખો, additional concession આપવું હોય ત્યારે જ વધારો. નોંધાયેલ discount નવી receiptથી ઓછું થતું નથી.",\n  agreementNeeded: "Payment નોંધતા પહેલા આ studentની નક્કી થયેલી fee set કરો.",\n  openLedger: "Full fees ledger ખોલો",\n  admissionSetupTitle: "આ admissionની fees",\n  admissionSetupHint: "આ studentની fee agreement અહીં set કરો અને આજે રકમ મળી હોય તો payment પણ નોંધો. Course default વાપરવું હોય તો agreement fields blank રાખો.",\n  courseDefaultNote: "Course defaults available હોય તો અહીં દેખાય છે. અહીં કરેલો ફેરફાર ફક્ત આ student માટે છે.",\n  save: "Payment નોંધો",')
          replace(p,
          '    belowReceived: "નક્કી થયેલી કુલ રકમ, અત્યાર સુધી મળેલી રકમ કરતાં ઓછી ન હોઈ શકે.",\n    generic:',
          '    belowReceived: "નક્કી થયેલી કુલ રકમ, અત્યાર સુધી મળેલી રકમ કરતાં ઓછી ન હોઈ શકે.",\n    agreementMissing: "પહેલા આ studentની નક્કી થયેલી fee set કરો, પછી payment નોંધો.",\n    discountDecrease: "નોંધાયેલ discount નવી receiptથી ઓછું કરી શકાતું નથી. Terms સુધારવા audited agreement change વાપરો.",\n    generic:')

          # Fees page: simple status selector, urgency sorting, no nested click
          # before the daily payment form.
          p = "src/app/admin/(console)/fees/page.tsx"
          replace(p,
          'type Props = { searchParams: Promise<{ q?: string; pending?: string }> };',
          'type Props = { searchParams: Promise<{ q?: string; status?: string }> };')
          replace(p,
          '''  const q = typeof params.q === "string" ? params.q.trim().toLowerCase().slice(0, 120) : "";
  const pendingOnly = params.pending === "1";''',
          '''  const q = typeof params.q === "string" ? params.q.trim().toLowerCase().slice(0, 120) : "";
  const statusFilter = ["pending", "overdue", "paid"].includes(params.status ?? "")
    ? params.status as "pending" | "overdue" | "paid"
    : "all";''')
          old = '''  const cards = enrollments.map((enrollment) => {
    const rows = byEnrollment.get(enrollment.enrollmentId) ?? [];
    const summary = summariseFees(
      {
        agreedFeeTotal: enrollment.agreedFeeTotal,
        agreedAdmissionAmount: enrollment.agreedAdmissionAmount,
        agreedBalanceDueOn: enrollment.agreedBalanceDueOn
      },
      rows
    );
    return { ...enrollment, rows, latest: rows[0] ?? null, summary, overdue: isOverdue(summary, today) };
  }).filter((card) => {
    if (pendingOnly && card.summary.balance <= 0) return false;
    if (!q) return true;
    return [card.fullName, card.admissionNo, card.phone, card.whatsapp ?? "", card.batchLabel].some((value) => value.toLowerCase().includes(q));
  });

  const totalNet = cards.reduce((sum, card) => sum + card.summary.net, 0);
  const totalReceived = cards.reduce((sum, card) => sum + card.summary.received, 0);
  const totalDue = cards.reduce((sum, card) => sum + card.summary.balance, 0);'''
          new = '''  const allCards = enrollments.map((enrollment) => {
    const rows = byEnrollment.get(enrollment.enrollmentId) ?? [];
    const summary = summariseFees(
      {
        agreedFeeTotal: enrollment.agreedFeeTotal,
        agreedAdmissionAmount: enrollment.agreedAdmissionAmount,
        agreedBalanceDueOn: enrollment.agreedBalanceDueOn
      },
      rows
    );
    return { ...enrollment, rows, latest: rows[0] ?? null, summary, overdue: isOverdue(summary, today) };
  });

  const totalNet = allCards.reduce((sum, card) => sum + card.summary.net, 0);
  const totalReceived = allCards.reduce((sum, card) => sum + card.summary.received, 0);
  const totalDue = allCards.reduce((sum, card) => sum + card.summary.balance, 0);
  const overdueCount = allCards.filter((card) => card.overdue).length;

  const cards = allCards
    .filter((card) => {
      if (statusFilter === "pending" && card.summary.balance <= 0) return false;
      if (statusFilter === "overdue" && !card.overdue) return false;
      if (statusFilter === "paid" && (card.summary.unset || card.summary.balance > 0)) return false;
      if (!q) return true;
      return [card.fullName, card.admissionNo, card.phone, card.whatsapp ?? "", card.batchLabel, card.courseNameEn, card.courseNameGu]
        .some((value) => value.toLowerCase().includes(q));
    })
    .sort((a, b) => Number(b.overdue) - Number(a.overdue) || b.summary.balance - a.summary.balance || a.fullName.localeCompare(b.fullName));'''
          replace(p, old, new)
          replace(p,
          '''        <div>
          <span className="kv-label">{copy.totalPending}</span>
          <span className="kv-value data-num">{money(totalDue)}</span>
        </div>
      </div>''',
          '''        <div>
          <span className="kv-label">{copy.totalPending}</span>
          <span className="kv-value data-num">{money(totalDue)}</span>
        </div>
        <div>
          <span className="kv-label">{copy.overdueCount}</span>
          <span className="kv-value data-num">{overdueCount}</span>
        </div>
      </div>''')
          replace(p,
          '''      <form method="get" className="toolbar mt-3 grid-cols-[1fr_auto] md:grid-cols-[1fr_auto_auto] md:items-end">
        <Field label={copy.search} htmlFor="fee-search"><input id="fee-search" name="q" className="input" defaultValue={params.q ?? ""} placeholder={copy.searchPlaceholder} /></Field>
        <label className="choice-chip text-smallmeta"><input type="checkbox" name="pending" value="1" className="size-4 accent-vermilion" defaultChecked={pendingOnly} />{copy.pendingOnly}</label>
        <button className="btn btn-primary" type="submit">{copy.show}</button>
      </form>''',
          '''      <form method="get" className="toolbar mt-3 grid-cols-[1fr_auto] md:grid-cols-[1fr_14rem_auto] md:items-end">
        <Field label={copy.search} htmlFor="fee-search"><input id="fee-search" name="q" className="input" defaultValue={params.q ?? ""} placeholder={copy.searchPlaceholder} /></Field>
        <Field label={copy.filterStatus} htmlFor="fee-status">
          <select id="fee-status" name="status" className="input" defaultValue={statusFilter}>
            <option value="all">{copy.filterAll}</option>
            <option value="pending">{copy.filterPending}</option>
            <option value="overdue">{copy.filterOverdue}</option>
            <option value="paid">{copy.filterPaid}</option>
          </select>
        </Field>
        <button className="btn btn-primary" type="submit">{copy.show}</button>
      </form>''')
          replace(p,
          '''              {canManage ? (
                <details className="border-t border-rule pt-5">
                  <summary className="cursor-pointer font-semibold">{copy.recordPayment}</summary>
                  <div className="mt-5"><FeeEntryForm enrollmentId={card.enrollmentId} courseFee={card.summary.agreed ?? 0} discount={card.summary.discount} dueDate={card.summary.nextDueOn} copy={copy} /></div>
                </details>
              ) : null}

              {canManage ? (
                <details className="border-t border-rule pt-5">
                  <summary className="cursor-pointer font-semibold">{copy.editAgreement}</summary>''',
          '''              {canManage && !card.summary.unset ? (
                <section className="border-t border-rule pt-5" aria-labelledby={`payment-${card.enrollmentId}`}>
                  <h4 id={`payment-${card.enrollmentId}`} className="font-semibold">{copy.recordPayment}</h4>
                  <div className="mt-4"><FeeEntryForm enrollmentId={card.enrollmentId} discount={card.summary.discount} dueDate={card.summary.nextDueOn} copy={copy} /></div>
                </section>
              ) : null}
              {canManage && card.summary.unset ? <p className="alert alert-warn">{copy.agreementNeeded}</p> : null}

              {canManage ? (
                <details className="border-t border-rule pt-5" open={card.summary.unset}>
                  <summary className="cursor-pointer font-semibold">{copy.editAgreement}</summary>''')

          # -----------------------------------------------------------------
          # Direct admission: fee agreement + first receipt can be captured in
          # the SAME transaction, but only for staff with fees.manage.
          # -----------------------------------------------------------------
          p = "src/app/admin/(console)/students/StudentForms.tsx"
          replace(p,
          'import { useActionState, useEffect, useRef } from "react";',
          'import { useActionState, useEffect, useRef, useState } from "react";')
          replace(p,
          'import type { StudentsCopy } from "@/lib/admin/students-copy";',
          'import type { StudentsCopy } from "@/lib/admin/students-copy";\nimport type { FeesCopy } from "@/lib/admin/fees-copy";\nimport { FEE_METHODS } from "@/lib/admin/fees";')
          replace(p,
          'export type BatchOption = { id: number; label: string; courseName: string; seats: number; seatsTaken: number; status: string };',
          'export type BatchOption = { id: number; label: string; courseName: string; seats: number; seatsTaken: number; status: string; feeTotal: number | null; feeAdmission: number | null; feeBalanceDueDays: number | null };')

          text = read(p)
          start = text.index('export function DirectAdmissionForm')
          end = text.index('export function ConvertEnquiryForm', start)
          direct = r'''export function DirectAdmissionForm({ batches, copy, feeCopy }: { batches: BatchOption[]; copy: StudentsCopy; feeCopy?: FeesCopy }) {
  const [state, action] = useActionState<StudentsState, FormData>(directAdmissionAction, IDLE);
  const formRef = useRef<HTMLFormElement>(null);
  const [selectedBatchId, setSelectedBatchId] = useState<number | null>(null);
  const selectedBatch = batches.find((batch) => batch.id === selectedBatchId) ?? null;

  useEffect(() => {
    if (!formRef.current || (!state.values && !state.invalidFields)) return;
    const frame = requestAnimationFrame(() => {
      if (formRef.current) restoreSubmittedForm(formRef.current, state.values, state.invalidFields);
    });
    const restoredBatchId = Number(state.values?.batchId ?? "");
    if (Number.isInteger(restoredBatchId) && restoredBatchId > 0) setSelectedBatchId(restoredBatchId);
    return () => cancelAnimationFrame(frame);
  }, [state.values, state.invalidFields]);

  return (
    <form ref={formRef} action={action} className="grid gap-5">
      <Message state={state} copy={copy} />
      <PersonFields copy={copy} requireGuardian />
      <div className="grid gap-4 md:grid-cols-2">
        <BatchField batches={batches} copy={copy} id="direct-batch" onChange={setSelectedBatchId} />
        <Field label={copy.joinedOn} htmlFor="direct-joined"><input id="direct-joined" name="joinedOn" type="date" className="input" /></Field>
      </div>
      {feeCopy ? <AdmissionFeeSetup key={selectedBatchId ?? "none"} batch={selectedBatch} copy={feeCopy} /> : null}
      <div><Submit label={copy.createAdmission} busy={copy.saving} /></div>
    </form>
  );
}

function AdmissionFeeSetup({ batch, copy }: { batch: BatchOption | null; copy: FeesCopy }) {
  return (
    <section className="panel">
      <div className="panel-head"><h3 className="text-h4">{copy.admissionSetupTitle}</h3></div>
      <div className="panel-body grid gap-4">
        <p className="form-note">{copy.admissionSetupHint}</p>
        <div className="grid gap-4 sm:grid-cols-3">
          <Field label={copy.agreedFeeTotal} htmlFor="direct-fee-total">
            <input id="direct-fee-total" name="agreedFeeTotal" type="number" min={0} step={1} className="input" defaultValue={batch?.feeTotal ?? ""} inputMode="numeric" />
          </Field>
          <Field label={copy.agreedAdmissionAmount} htmlFor="direct-fee-admission">
            <input id="direct-fee-admission" name="agreedAdmissionAmount" type="number" min={0} step={1} className="input" defaultValue={batch?.feeAdmission ?? ""} inputMode="numeric" />
          </Field>
          <Field label={copy.agreedBalanceDueOn} htmlFor="direct-fee-due-on">
            <input id="direct-fee-due-on" name="agreedBalanceDueOn" type="date" className="input" />
          </Field>
        </div>
        <p className="form-note">{copy.courseDefaultNote}</p>
        <div className="grid gap-4 sm:grid-cols-3">
          <Field label={copy.discountTotal} htmlFor="direct-fee-discount">
            <input id="direct-fee-discount" name="feeDiscount" type="number" min={0} step={1} className="input" defaultValue={0} inputMode="numeric" />
          </Field>
          <Field label={copy.receivedNow} htmlFor="direct-fee-received">
            <input id="direct-fee-received" name="feeReceived" type="number" min={0} step={1} className="input" defaultValue={0} inputMode="numeric" />
          </Field>
          <Field label={copy.method} htmlFor="direct-fee-method">
            <select id="direct-fee-method" name="feeMethod" className="input" defaultValue="">
              <option value="">—</option>{FEE_METHODS.map((method) => <option key={method} value={method}>{copy.methods[method]}</option>)}
            </select>
          </Field>
        </div>
        <details className="border-t border-rule pt-3">
          <summary className="cursor-pointer text-smallmeta font-semibold">{copy.moreOptions}</summary>
          <div className="mt-4 grid gap-4 sm:grid-cols-2">
            <Field label={copy.receiptNo} htmlFor="direct-fee-receipt"><input id="direct-fee-receipt" name="feeReceiptNo" className="input" maxLength={40} /></Field>
            <Field label={copy.notes} htmlFor="direct-fee-note"><input id="direct-fee-note" name="feeNote" className="input" maxLength={300} /></Field>
          </div>
        </details>
      </div>
    </section>
  );
}

'''
          write(p, text[:start] + direct + text[end:])

          replace(p,
          '''function BatchField({ batches, copy, id }: { batches: BatchOption[]; copy: StudentsCopy; id: string }) {
  return (
    <Field label={copy.selectBatch} htmlFor={id}>
      <select id={id} name="batchId" className="input" required defaultValue="">''',
          '''function BatchField({ batches, copy, id, onChange }: { batches: BatchOption[]; copy: StudentsCopy; id: string; onChange?: (batchId: number | null) => void }) {
  return (
    <Field label={copy.selectBatch} htmlFor={id}>
      <select id={id} name="batchId" className="input" required defaultValue="" onChange={(event) => onChange?.(event.target.value ? Number(event.target.value) : null)}>''')

          p = "src/app/admin/(console)/students/actions.ts"
          replace(p,
          'import { auditValues, STUDENT_AUDIT_ACTIONS } from "@/lib/admin/audit";',
          'import { auditValues, FEE_AUDIT_ACTIONS, STUDENT_AUDIT_ACTIONS } from "@/lib/admin/audit";')
          marker = 'import { pad } from "@/lib/utils";'
          replace(p, marker, marker + '\nimport { validateAdmissionFeeSetup, type AdmissionFeeSetupInput } from "@/lib/admin/fees";')
          replace(p,
          '''  const auth = await authorizeAction({ permission: "students.manage" });
  if (!auth.ok) return { ...errorState("denied"), values: submittedValues };

  const parsed = validateDirectAdmission(formObject(formData));''',
          '''  const auth = await authorizeAction({ permission: "students.manage" });
  if (!auth.ok) return { ...errorState("denied"), values: submittedValues };

  const feeFieldNames = ["agreedFeeTotal", "agreedAdmissionAmount", "agreedBalanceDueOn", "feeDiscount", "feeReceived", "feeMethod", "feeReceiptNo", "feeNote"];
  const hasFeeSetup = feeFieldNames.some((name) => formData.has(name));
  let feeSetup: AdmissionFeeSetupInput | null = null;
  if (hasFeeSetup) {
    const feeAuth = await authorizeAction({ permission: "fees.manage" });
    if (!feeAuth.ok) return { ...errorState("denied"), values: submittedValues };
    const feeParsed = validateAdmissionFeeSetup(formObject(formData));
    if (!feeParsed.ok) return { ...errorState("invalid"), values: submittedValues, invalidFields: feeFieldNames };
    feeSetup = feeParsed.value;
  }

  const parsed = validateDirectAdmission(formObject(formData));''')
          replace(p,
          '''      const agreement = await agreementForBatch(tx, d.batchId, joinDate);
      const enrollment = await tx
        .insert(schema.enrollments)
        .values({ studentId, batchId: d.batchId, status: "active", joinedOn: joinDate, ...agreement })''',
          '''      const agreement = await agreementForBatch(tx, d.batchId, joinDate);
      if (feeSetup) {
        agreement.agreedFeeTotal = feeSetup.agreedFeeTotal ?? agreement.agreedFeeTotal;
        agreement.agreedAdmissionAmount = feeSetup.agreedAdmissionAmount ?? agreement.agreedAdmissionAmount;
        agreement.agreedBalanceDueOn = feeSetup.agreedBalanceDueOn ?? agreement.agreedBalanceDueOn;
        if (agreement.agreedFeeTotal == null && (agreement.agreedAdmissionAmount != null || feeSetup.discount > 0 || feeSetup.received > 0)) {
          throw new FeeSetupInvalidError();
        }
        if (agreement.agreedFeeTotal != null) {
          if (agreement.agreedAdmissionAmount != null && agreement.agreedAdmissionAmount > agreement.agreedFeeTotal) throw new FeeSetupInvalidError();
          if (feeSetup.discount > agreement.agreedFeeTotal) throw new FeeSetupInvalidError();
          if (feeSetup.received > agreement.agreedFeeTotal - feeSetup.discount) throw new FeeSetupInvalidError();
          if (agreement.agreedAdmissionAmount != null && agreement.agreedAdmissionAmount >= agreement.agreedFeeTotal && feeSetup.agreedBalanceDueOn == null) {
            agreement.agreedBalanceDueOn = null;
          }
        }
      }
      const enrollment = await tx
        .insert(schema.enrollments)
        .values({ studentId, batchId: d.batchId, status: "active", joinedOn: joinDate, ...agreement })''')
          replace(p,
          '''      if (seat[0].status === "open" && seat[0].seatsTaken >= seat[0].seats) {
        await tx.update(schema.batches).set({ status: "full" }).where(eq(schema.batches.id, d.batchId));
      }

      await tx.insert(schema.auditLogs).values([''',
          '''      if (feeSetup && agreement.agreedFeeTotal != null && (feeSetup.received > 0 || feeSetup.discount > 0 || feeSetup.notes)) {
        const feeRows = await tx.insert(schema.feeRecords).values({
          enrollmentId,
          courseFee: agreement.agreedFeeTotal,
          discount: feeSetup.discount,
          received: feeSetup.received,
          method: feeSetup.method,
          receiptNo: feeSetup.receiptNo,
          dueDate: agreement.agreedBalanceDueOn,
          notes: feeSetup.notes
        }).returning({ id: schema.feeRecords.id });
        const feeRecordId = feeRows[0]?.id;
        if (!feeRecordId) throw new Error("direct admission fee insert returned no id");
        const receiptNo = feeSetup.receiptNo ?? (feeSetup.received > 0 ? `KDS-R-${joinDate.slice(0, 4)}-${pad(feeRecordId)}` : null);
        if (receiptNo && !feeSetup.receiptNo) await tx.update(schema.feeRecords).set({ receiptNo }).where(eq(schema.feeRecords.id, feeRecordId));
        await tx.insert(schema.auditLogs).values(auditValues({
          actor: String(auth.session.staff.id),
          action: FEE_AUDIT_ACTIONS.recordCreated,
          entity: "fee_record",
          entityId: feeRecordId,
          newValue: { enrollmentId, courseFee: agreement.agreedFeeTotal, discount: feeSetup.discount, received: feeSetup.received, method: feeSetup.method, receiptNo, dueDate: agreement.agreedBalanceDueOn },
          reason: feeSetup.notes ?? "fee recorded at direct admission"
        }));
      }

      if (seat[0].status === "open" && seat[0].seatsTaken >= seat[0].seats) {
        await tx.update(schema.batches).set({ status: "full" }).where(eq(schema.batches.id, d.batchId));
      }

      await tx.insert(schema.auditLogs).values([''')
          replace(p,
          '''  } catch (error) {
    if (error instanceof SeatUnavailableError) return { ...errorState("seat"), values: submittedValues };
    return { ...mapDbError(error, "[students] direct admission"), values: submittedValues };
  }''',
          '''  } catch (error) {
    if (error instanceof SeatUnavailableError) return { ...errorState("seat"), values: submittedValues };
    if (error instanceof FeeSetupInvalidError) return { ...errorState("invalid"), values: submittedValues, invalidFields: feeFieldNames };
    return { ...mapDbError(error, "[students] direct admission"), values: submittedValues };
  }''')
          replace(p,
          '''function revalidateStudentPaths() {
  revalidatePath("/admin/students");
  revalidatePath("/admin/courses");''',
          '''function revalidateStudentPaths() {
  revalidatePath("/admin/students");
  revalidatePath("/admin/fees");
  revalidatePath("/admin/courses");''')
          replace(p,
          'class SeatUnavailableError extends Error {}\nclass AlreadyConvertedError extends Error {}',
          'class SeatUnavailableError extends Error {}\nclass FeeSetupInvalidError extends Error {}\nclass AlreadyConvertedError extends Error {}')

          # Student screen: fee defaults reach Direct Admission, and a selected
          # student's small fee card leads to the full ledger.
          p = "src/app/admin/(console)/students/page.tsx"
          replace(p,
          'import { studentsCopy } from "@/lib/admin/students-copy";',
          'import { studentsCopy } from "@/lib/admin/students-copy";\nimport { feesCopy } from "@/lib/admin/fees-copy";')
          replace(p,
          '''  const canView = hasPermission(session.staff, "students.view") || hasPermission(session.staff, "students.manage");
  const canManage = hasPermission(session.staff, "students.manage");''',
          '''  const canView = hasPermission(session.staff, "students.view") || hasPermission(session.staff, "students.manage");
  const canManage = hasPermission(session.staff, "students.manage");
  const canViewFees = hasPermission(session.staff, "fees.view") || hasPermission(session.staff, "fees.manage");
  const canManageFees = hasPermission(session.staff, "fees.manage");''')
          replace(p,
          '  const copy = studentsCopy(session.staff.adminLocale);',
          '  const copy = studentsCopy(session.staff.adminLocale);\n  const feeCopy = feesCopy(session.staff.adminLocale);')
          replace(p,
          '''      courseNameEn: schema.courses.nameEn,
      courseNameGu: schema.courses.nameGu
    }).from(schema.batches)''',
          '''      courseNameEn: schema.courses.nameEn,
      courseNameGu: schema.courses.nameGu,
      feeTotal: schema.courses.feeTotal,
      feeAdmission: schema.courses.feeAdmission,
      feeBalanceDueDays: schema.courses.feeBalanceDueDays
    }).from(schema.batches)''')
          replace(p,
          '''    seats: batch.seats,
    seatsTaken: batch.seatsTaken,
    status: batch.status
  }));''',
          '''    seats: batch.seats,
    seatsTaken: batch.seatsTaken,
    status: batch.status,
    feeTotal: batch.feeTotal,
    feeAdmission: batch.feeAdmission,
    feeBalanceDueDays: batch.feeBalanceDueDays
  }));''')
          replace(p,
          '<div className="panel-body border-t border-rule"><p className="form-note mb-3">{copy.directAdmissionHint}</p><DirectAdmissionForm batches={batches} copy={copy} /></div>',
          '<div className="panel-body border-t border-rule"><p className="form-note mb-3">{copy.directAdmissionHint}</p><DirectAdmissionForm batches={batches} copy={copy} feeCopy={canManageFees ? feeCopy : undefined} /></div>')
          replace(p,
          '''                <div className="panel"><div className="panel-head"><h3 className="text-h4">{copy.fees}</h3></div><div className="panel-body"><div className="grid grid-cols-2 gap-4"><Fact label={copy.paid} value={formatInr(feeSummary.received)} /><Fact label={copy.due} value={formatInr(feeSummary.due)} /></div>{fees.length === 0 ? <p className="form-note mt-4">{copy.noFees}</p> : <p className="form-note mt-4">{fees.length} ledger entr{fees.length === 1 ? "y" : "ies"}</p>}</div></div>''',
          '''                <div className="panel">
                  <div className="panel-head"><h3 className="text-h4">{copy.fees}</h3></div>
                  <div className="panel-body">
                    <div className="grid grid-cols-2 gap-4"><Fact label={copy.paid} value={formatInr(feeSummary.received)} /><Fact label={copy.due} value={formatInr(feeSummary.due)} /></div>
                    {fees.length === 0 ? <p className="form-note mt-4">{copy.noFees}</p> : <p className="form-note mt-4">{fees.length} ledger entr{fees.length === 1 ? "y" : "ies"}</p>}
                    {canViewFees ? <p className="mt-4"><Link className="btn btn-secondary" href={`/admin/fees?q=${encodeURIComponent(selected.admissionNo)}`}>{feeCopy.openLedger}</Link></p> : null}
                  </div>
                </div>''')

          # Navigation: the four daily phone destinations are Today,
          # Admissions, Students, Fees. Everything else remains in More/rail.
          p = "src/app/admin/(console)/layout.tsx"
          replace(p,
          '''    { tab: { href: "/admin/students", label: t("nav.students"), icon: "people" }, allowed: canUseStudents },
    { tab: { href: "/admin/batches", label: t("nav.batches"), icon: "calendar" }, allowed: canUseBatches },
    { tab: { href: "/admin/fees", label: t("nav.fees"), icon: "check" }, allowed: canUseFees },''',
          '''    { tab: { href: "/admin/students", label: t("nav.students"), icon: "people" }, allowed: canUseStudents },
    { tab: { href: "/admin/fees", label: t("nav.fees"), icon: "check" }, allowed: canUseFees },
    { tab: { href: "/admin/batches", label: t("nav.batches"), icon: "calendar" }, allowed: canUseBatches },''')

          p = "src/app/admin/(console)/page.tsx"
          text = read(p)
          start = text.index('  const quickActions = [')
          end = text.index('  ].filter((item): item is { href: string; label: string } => item !== null);', start) + len('  ].filter((item): item is { href: string; label: string } => item !== null);')
          quick = '''  const quickActions = [
    hasPermission(session.staff, "applications.manage") ? { href: "/admin/admissions", label: t("nav.admissions") } : null,
    hasPermission(session.staff, "students.manage") ? { href: "/admin/students", label: t("nav.students") } : null,
    hasPermission(session.staff, "fees.manage") ? { href: "/admin/fees", label: t("permissions.groups.fees") } : null,
    hasPermission(session.staff, "attendance.manage") ? { href: "/admin/attendance", label: t("nav.attendance") } : null
  ].filter((item): item is { href: string; label: string } => item !== null);'''
          write(p, text[:start] + quick + text[end:])

          # Focused regression tests for the new contracts.
          Path("tests/front-desk-fees-ux.test.ts").write_text(r'''import { describe, expect, it } from "vitest";
import { readFileSync } from "node:fs";
import { join } from "node:path";
import { validateAdmissionFeeSetup, validatePaymentEntry } from "@/lib/admin/fees";

const read = (path: string) => readFileSync(join(process.cwd(), path), "utf8");

describe("front desk fee input", () => {
  it("records a daily payment without accepting the agreed course fee from the receipt form", () => {
    const parsed = validatePaymentEntry({ enrollmentId: "7", received: "5000", method: "cash", discount: "1000" });
    expect(parsed.ok).toBe(true);
    if (parsed.ok) expect(parsed.value).toMatchObject({ enrollmentId: 7, received: 5000, discount: 1000, method: "cash" });
    expect(validatePaymentEntry({ enrollmentId: "7", received: "5000", method: "" }).ok).toBe(false);
  });

  it("accepts a full per-student fee setup at direct admission", () => {
    const parsed = validateAdmissionFeeSetup({
      agreedFeeTotal: "30000",
      agreedAdmissionAmount: "10000",
      agreedBalanceDueOn: "2026-10-15",
      feeDiscount: "2000",
      feeReceived: "10000",
      feeMethod: "upi",
      feeReceiptNo: "PAPER-18",
      feeNote: "owner-approved concession"
    });
    expect(parsed.ok).toBe(true);
    if (parsed.ok) expect(parsed.value.received).toBe(10000);
  });
});

describe("admission recovery", () => {
  it("revalidates restored course choices and surfaces typed API errors", () => {
    const source = read("src/components/forms/AdmissionForm.tsx");
    expect(source).toContain("resetToCourseStep");
    expect(source).toContain("restoredCourse.scheduleOptions.some");
    expect(source).toContain("Object.assign({}, validate(0), validate(1), validate(2), validate(3))");
    expect(source).toContain("out.error === \"validation\"");
    expect(source).toContain("serverError.requestId");
  });
});

describe("non-technical staff navigation", () => {
  it("puts Fees in the four daily mobile destinations before Batches", () => {
    const layout = read("src/app/admin/(console)/layout.tsx");
    expect(layout.indexOf('href: "/admin/fees"')).toBeLessThan(layout.indexOf('href: "/admin/batches"'));
  });

  it("keeps the Today shortcut strip to the four front-desk jobs", () => {
    const page = read("src/app/admin/(console)/page.tsx");
    const start = page.indexOf("const quickActions = [");
    const end = page.indexOf("].filter", start);
    const quick = page.slice(start, end);
    expect(quick).toContain("applications.manage");
    expect(quick).toContain("students.manage");
    expect(quick).toContain("fees.manage");
    expect(quick).toContain("attendance.manage");
    expect(quick).not.toContain("courses.manage");
    expect(quick).not.toContain("design.manage");
    expect(quick).not.toContain("content.manage");
  });

  it("requires fees.manage before a direct admission can write money", () => {
    const actions = read("src/app/admin/(console)/students/actions.ts");
    expect(actions).toContain('authorizeAction({ permission: "fees.manage" })');
    expect(actions).toContain("FEE_AUDIT_ACTIONS.recordCreated");
    expect(actions).toContain("fee recorded at direct admission");
  });

  it("keeps the daily payment form free of an editable course-fee input", () => {
    const form = read("src/app/admin/(console)/fees/FeeForm.tsx");
    const start = form.indexOf("export function FeeEntryForm");
    const end = form.indexOf("function Field", start);
    const payment = form.slice(start, end);
    expect(payment).not.toContain('name="courseFee"');
    expect(payment).toContain('name="received"');
    expect(payment).toContain('name="method"');
  });
});
''')

          # Keep durable project memory truthful without turning a UI pass into
          # an architecture rewrite.
          p = "docs/project-context.md"
          text = read(p)
          note = '''\n\n### Front-desk fee workflow (2026-09-03)\n\nKarma Console treats fees as a front-desk ledger, not an ERP payment module. A direct admission may capture that student’s agreed total, admission amount, balance date, concession and first offline receipt in the same transaction when the operator also has `fees.manage`. Daily payment entry never accepts the agreed course fee from the receipt form; it derives the agreement from the enrolment snapshot, so recording cash cannot accidentally reprice a student. Fee status remains derived from the immutable ledger. The mobile primary destinations prioritise Today → Admissions → Students → Fees; specialist modules remain in More / the desktop rail.\n'''
          if "### Front-desk fee workflow (2026-09-03)" not in text:
              write(p, text.rstrip() + note + "\n")

          print("patched files:")
          import subprocess
          subprocess.run(["git", "diff", "--stat"], check=True)
          PY
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Verify patch
        run: |
          npm run typecheck
          npm run lint
          npm test
      - name: Commit patch
        shell: bash
        run: |
          git config user.name "optimisticjp"
          git config user.email "optimistic.jp@gmail.com"
          git add messages src tests docs/project-context.md
          git diff --cached --check
          git commit -m "feat: simplify front desk fees and admission recovery"
          git push origin HEAD:feat/fees-and-console-ux
